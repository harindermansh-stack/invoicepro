name: 🎭 Playwright E2E tests
on:
  deployment_status:

# The concurrency block ensures that only one Playwright E2E test workflow runs per branch or pull request at a time.
# If a new run is triggered (for example, by a commit push), any currently running workflow for that same branch will be cancelled.
concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  e2e:
    # https://vercel.com/guides/how-can-i-run-end-to-end-tests-after-my-vercel-preview-deployment
    if: github.event_name == 'deployment_status' && github.event.deployment_status.state == 'success'

    name: Run e2e tests via ${{ github.event_name == 'deployment_status' && 'Vercel deployment' }}
    timeout-minutes: 50
    # we use macos-latest because I am using mac os locally and generating snapshots (screenshots) for the PDF tests locally, so we need to use the same OS on the CI. https://docs.github.com/en/actions/how-tos/write-workflows/choose-where-workflows-run/choose-the-runner-for-a-job#choosing-github-hosted-runners
    runs-on: macos-latest
    env:
      TZ: Europe/Warsaw # we use Europe/Warsaw timezone for consistent date handling across local machines and CI
      EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
      EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
    steps:
      - name: Echo environment URL
        env:
          ENVIRONMENT_URL: ${{ github.event_name == 'deployment_status' && github.event.deployment_status.environment_url }}
        run: |
          echo "Environment URL - $ENVIRONMENT_URL"
      # we use pinned versions because there are safer to use: https://x.com/paulmillr/status/1900948425325031448
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        name: 🛎️ Checkout repository
        with:
          persist-credentials: false

      - uses: SocketDev/action@ba6de6cc0565af1f42295590380973573297e31f # v1.3.2
        name: 🔒 Setup Socket Firewall
        with:
          mode: firewall-free

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        name: 📚 Setup Node.js
        with:
          node-version: lts/*

      - name: 📦 Enable Corepack
        run: corepack enable

      - name: 📦 Install pinned package manager # from package.json
        run: corepack install

      - name: 🚚 Install dependencies
        run: sfw pnpm install --frozen-lockfile # firewall to block malicious packages from being installed https://docs.socket.dev/docs/socket-firewall-overview

      - name: 📦 Install Playwright browser binaries & OS dependencies
        run: pnpm exec playwright install chromium webkit --with-deps

      - name: 🎭 Run Playwright tests
        id: playwright
        env:
          BASE_URL: ${{ github.event_name == 'deployment_status' && github.event.deployment_status.environment_url }}
          SENTRY_ENABLED: false
          NEXT_PUBLIC_SENTRY_ENABLED: false
        continue-on-error: true
        # https://playwright.dev/docs/test-reporters#line-reporter
        run: pnpm exec playwright test --reporter=html,list

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        name: 🎲 Upload Playwright report
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-output/
          if-no-files-found: ignore
          retention-days: 1

      - name: 🔍 Get PR URL
        id: pr-url
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPOSITORY: ${{ github.repository }}
          GH_REPOSITORY_OWNER: ${{ github.repository_owner }}
          GH_REF_NAME: ${{ github.ref_name }}
        run: |
          PR_URL=$(curl -s \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/$GH_REPOSITORY/pulls?head=$GH_REPOSITORY_OWNER:$GH_REF_NAME" \
            | jq -r '.[0].html_url // empty')
          echo "pr_url=$PR_URL" >> "$GITHUB_OUTPUT"

      - name: 📧 Send email
        ## always notify; subject/body reflect cancellation vs pass/fail
        if: always() && env.EMAIL_USERNAME != '' && env.EMAIL_PASSWORD != '' && contains(env.EMAIL_USERNAME, '@')
        continue-on-error: true
        # https://github.com/dawidd6/action-send-mail
        uses: dawidd6/action-send-mail@d38f3f7cd391cdebfe0d38efc3998b935e951c4f # v16
        with:
          server_address: smtp.gmail.com
          server_port: 587
          from: "GitHub Actions (no-reply) <${{ env.EMAIL_USERNAME }}>"
          to: ${{ env.EMAIL_USERNAME }}
          username: ${{ env.EMAIL_USERNAME }}
          password: ${{ env.EMAIL_PASSWORD }}
          subject: ${{ job.status == 'cancelled' && '⚠️ E2E Tests Cancelled' || (steps.playwright.outcome == 'success' && '✅ E2E Tests Passed' || '❌ E2E Tests Failed') }} for ${{ github.repository }}
          body: |
            E2E tests for ${{ github.repository }} have completed.

            Result: ${{ job.status == 'cancelled' && '⚠️ Cancelled' || (steps.playwright.outcome == 'success' && '✅ Passed' || '❌ Failed') }}

            For more details, please check:
            - GitHub Actions run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
            ${{ steps.pr-url.outputs.pr_url && format('- Pull Request: {0}', steps.pr-url.outputs.pr_url) }}
            - Vercel deployment: ${{ github.event.deployment_status.environment_url }}
          attachments: ${{ job.status != 'cancelled' && 'playwright-output/report/index.html' || '' }}

      - name: ❌ Mark workflow failed on E2E Tests failure
        if: steps.playwright.outcome == 'failure'
        run: exit 1
